"""Built-in provider implementations.

These satisfy the `LLMProvider` protocol from `rosenix.protocols`
but do not need to be used — any class with the same shape works
equally well. They exist for convenience, not as the "correct" way to
add a provider.
"""
